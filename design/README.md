# Design Prototypes

This folder is not the production app.

- `dashboard/public/` is the live Firebase Hosting source.
- `Vaahana/` is the shipped iOS app source.
- Files in `design/` are archived mockups and prototype copies kept only for reference.

Do not ship or patch `design/*` as part of production fixes unless you are intentionally updating archived concepts.
